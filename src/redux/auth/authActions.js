const initialState = {
  logInState: false,
};

const authReducer = (state = initialState, action) => {
  switch (action.type) {
    case "LogIn":
      break;
    case "LogOut":
      break;
    default:
      break;
  }
};

export default authReducer;
